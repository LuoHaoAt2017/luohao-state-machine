// import Service from './service';

export function register(store, options) {
  // const objectId = options.ObjectId;
  // options.UpdateView = function(mutation, playload) {
  //   store.commit(`${objectId}/${mutation}`, playload);
  // }
  // return Service.newInstance(options);
}
